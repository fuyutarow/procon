use itertools::Itertools;
use proconio::marker::Chars;
use proconio::{fastout, input};
use std::cmp::{max, min};
use std::collections::{HashMap, HashSet, VecDeque};
use std::mem::swap;

#[fastout]
fn main() {
    input! {
        H: usize,
        W: usize,
        K: usize,
        graph: [ Chars; H],
    }


    for r in 0..2_usize.pow(H as u32) {
        for c in 0..2_usize.pow(W as u32) {
            
        }
    }
}
